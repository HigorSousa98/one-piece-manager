{
  "name": "one-piece-world",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "setup": "npm --prefix one-piece-manager install",
    "dev": "cd one-piece-manager && npm run dev",
    "build": "cd one-piece-manager && npm run build",
    "preview": "cd one-piece-manager && npm run preview"
  }
}
